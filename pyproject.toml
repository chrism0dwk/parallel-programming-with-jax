[project]
name = "parallelcomputingwithjax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chris Jewell", email = "chrism0dwk@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "distrax>=0.1.9",
    "jax>=0.11.1",
    "jupyterlab>=4.6.3",
    "matplotlib>=3.11.1",
    "numpy>=2.5.3",
]

[project.scripts]
parallelcomputingwithjax = "parallelcomputingwithjax:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
